; folds.scm - BMB code folding queries

; Function bodies
(function_definition) @fold

; Struct definitions
(struct_definition) @fold

; Enum definitions
(enum_definition) @fold

; Block expressions
(block_expression) @fold

; Match expressions
(match_expression) @fold

; While loops
(while_expression) @fold

; For loops
(for_expression) @fold

; Where blocks (contracts)
(where_block) @fold
